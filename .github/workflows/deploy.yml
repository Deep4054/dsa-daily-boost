name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Build
      run: npm run build
      env:
        VITE_SUPABASE_URL: https://lsgjhhkbroecvlmiolnc.supabase.co
        VITE_SUPABASE_ANON_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImxzZ2poaGticm9lY3ZsbWlvbG5jIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTY2Mjc4NTEsImV4cCI6MjA3MjIwMzg1MX0.wekYdQZERszqA552XY8dW8YjH_2s19Pp6_5eFSARCvk
        VITE_GOOGLE_CLIENT_ID: 251902222844-i2iaij37a3079dcramcabm8tk7mis1tk.apps.googleusercontent.com
        VITE_APP_URL: https://Deep4054.github.io/dsa-daily-boost
        
    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v4
      with:
        github_token: ${{ github.token }}
        publish_dir: ./dist